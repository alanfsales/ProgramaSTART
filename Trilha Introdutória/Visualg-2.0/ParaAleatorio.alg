algoritmo "ParaAleatorio"

var
i, aleatorioMax, alealorioMin, incremento: inteiro
inicio

repita
   aleatorio 1,100
       leia(aleatorioMax)
       leia(alealorioMin)
   aleatorio off
ate aleatorioMax >= alealorioMin
limpatela

aleatorio 1,5
     leia(incremento)
aleatorio off
limpatela

para i de alealorioMin ate aleatorioMax passo incremento faca
     escreval(i)
fimpara

fimalgoritmo
